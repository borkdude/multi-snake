{:paths ["src"]
 ;; This branch tracks the request-mark branch of a Buzz checkout next door.
 :deps {io.github.borkdude/buzz {:local/root "../buzz"}}
 :tasks {serve {:doc "Play on http://localhost:1350"
                :requires ([snake.main :as main])
                :task (main/-main)}
         dev   {:doc "The same, plus an nrepl on 1668"
                :requires ([snake.main :as main])
                :task (main/-main "--nrepl")}}}
